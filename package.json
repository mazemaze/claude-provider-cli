{
  "name": "claude-provider-cli",
  "version": "0.1.0",
  "description": "CLI for switching Claude Code providers with macOS Keychain-backed secrets",
  "bin": {
    "claude-provider": "./bin/claude-provider.js",
    "ccp": "./bin/ccp.js"
  },
  "type": "commonjs",
  "scripts": {
    "test": "node ./test/smoke.test.js"
  }
}
